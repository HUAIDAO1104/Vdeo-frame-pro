# Windows 0.2.1 发布验证

发布于 2026-09-15 17:44（Asia/Shanghai），正式版已标记 GitHub Latest。

- [Release](https://github.com/HUAIDAO1104/Vdeo-frame-pro/releases/tag/desktop-v0.2.1)
- [Windows CI 构建 34952549901](https://github.com/HUAIDAO1104/Vdeo-frame-pro/actions/runs/34952549901)：成功，构建任务用时 10 分 36 秒。
- 标签：`desktop-v0.2.1`。
- 构建提交：`df92f78a20453efb881f0ad50afe97c0bcc20309`。
- 安装包：`SalesKitStudio_0.2.1_x64-setup.exe`，60,364,845 字节。
- SHA-256：`f5acc1a4262f1824d1215e34c5b43e195794661e8ebca1977a89d241abb457d8`。

## 验证结果

1. Windows CI 中 45 项 JavaScript 测试和 2 项 Rust 测试通过，发行构建成功。
2. Windows CI 中实际静默安装，内置 FFmpeg 和 ffprobe 均可正常运行；启动应用后等待 10 秒，进程保持运行。
3. 下载最终安装包并核对 SHA-256，与校验文件、构建记录和 GitHub 资产摘要一致；构建提交、版本及运行编号与发行标签匹配。
4. 从已发布的 0.2.0 源码标签提取更新公钥，确认与 0.2.1 公钥及应用标识一致；使用该旧版公钥独立验证 0.2.1 安装包 minisign 签名，通过。
5. 发布后，无需认证即可获取 `releases/latest/download/latest.json`，与已验证清单完全一致。清单版本为 0.2.1，签名与 `.sig` 文件一致，指向正式安装包；公开安装包可访问并返回 Windows 可执行文件头。
6. 本地 13 组浏览器场景通过，含真实 JPEG 解码与 Canvas 合成：210 张合成候选归并为 70 张代表帧，模拟评分接口收到全部 70 张（9 批），自动成图只使用代表帧，原始 210 张仍保留，统计可序列化到历史。
7. 规则测试覆盖超过 32/60 个不同镜头、连续重复画面、相同亮度不同颜色、局部变化、缓慢运镜、返回先前镜头、时间缺口和代表帧不足时的布局。Web 与离线桌面前端构建通过。

## 更新方式与边界

0.2.0 已具备更新模块，可在设置中检查并执行“保存并安装”；运行或暂停中的任务须先停止。0.1.6 及更早版本需手动安装新版一次。

此次验证了旧版公钥对新版成品的签名兼容、公开更新清单，以及 Windows 全新安装与启动；未把用户实际电脑上的覆盖升级描述为已完成。本地选图测试使用合成图像与模拟 AI 评分，未调用付费模型，不代表真实素材上的审美效果已通过验收。

归并仍基于已提取候选，原有抽帧范围、间隔、模式和 600 帧上限保持不变。本次发布不更换模型，也不包含此前审查报告中其余未实施事项。安装包未使用 Windows Authenticode 发布者证书签名。

发布后同步下载入口与此文档的提交，不改变发行标签或安装包。
