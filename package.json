{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test tests/product-contracts.test.mjs",
    "build": "vite build",
    "desktop:frontend": "node scripts/build-desktop-frontend.mjs",
    "desktop:dev:frontend": "vite --config vite.desktop.config.ts",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:info": "tauri info",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@tauri-apps/cli": "^2.8.4",
    "playwright": "^1.60.0",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
